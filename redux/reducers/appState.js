const INITIAL_STATE = {
    env: 'production'
};

export default appStaterReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {

        default:
            return state
    }
};
